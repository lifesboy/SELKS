input {
  file {
    path => ["/var/log/nginx/*.log"]
    sincedb_path => ["/var/cache/logstash/sincedbs/selks-nginx.db"]
    type => "selks-nginx"
  }
}

filter {
  #if [type] == "ray-session" {
  #  grok {
  #    match => [
  #       "message", "%{LOGLEVEL:loglevel} - %{NOTSPACE:taskid}
  #          - %{NOTSPACE:logger} - %{WORD:label}( - %{INT:duration:int})?"
  #    ]
  # }
  #  date {
  #      match => [ "timestamp", "UNIX" ]
  #  }
  #}
}

output {
  if [type] == "selks-nginx" {
    elasticsearch {
        hosts => "127.0.0.1"
        index => "selks-nginx"
        template_overwrite => true
        template => "/etc/logstash/elasticsearch7-selks-template.json"
    }
  }
}
