input {
  file {
    path => ["/tmp/PHP_errors.log"]
    sincedb_path => ["/var/cache/logstash/sincedbs/opnsense-php.db"]
    type => "opnsense-php"
  }
}

filter {
  #if [type] == "ray-session" {
  #  grok {
  #    match => [
  #       "message", "%{LOGLEVEL:loglevel} - %{NOTSPACE:taskid}
  #          - %{NOTSPACE:logger} - %{WORD:label}( - %{INT:duration:int})?"
  #    ]
  # }
  #  date {
  #      match => [ "timestamp", "UNIX" ]
  #  }
  #}
}

output {
  if [type] == "opnsense-php" {
    elasticsearch {
        hosts => "127.0.0.1"
        index => "opnsense-php"
        template_overwrite => true
        template => "/etc/logstash/elasticsearch7-opnsense-php-template.json"
    }
  }
}
